# System Python (not Blender's bundled Python)
# Install:  pip install -r requirements.txt

# --- Live MediaPipe capture ---
mediapipe>=0.10.11
opencv-python>=4.8.0

# --- Orchestrator (TTS + playback + optional Claude) ---
sounddevice>=0.4.6
soundfile>=0.12.1
numpy>=1.24.0
anthropic>=0.28.0

# --- Parler-TTS (primary TTS engine; also needs torch + torchaudio matching torch) ---
#   pip install git+https://github.com/huggingface/parler-tts.git
#   pip install transformers==4.46.1 "tokenizers>=0.20.3,<0.21" accelerate
# torch / torchaudio: install matching CUDA/CPU builds from https://pytorch.org
# Pin: parler-tts 0.2.x needs transformers==4.46.1 which needs tokenizers 0.20.x
# (litellm may want tokenizers>=0.21 — prefer parler for orchestrator)
transformers==4.46.1
tokenizers>=0.20.3,<0.21
accelerate>=0.33.0

# --- Lip-sync: wav2arkit (audio → 52 ARKit blendshapes, CPU ONNX) ---
onnxruntime>=1.16.0
scipy>=1.10.0
# librosa optional (scipy used for fast resample)

# --- Brain path (LIPSYNC_ENGINE = "brain") ---
# torchcrepe: GPU pitch for prosody_gpu.py
torchcrepe>=0.0.22
# extract_emotion_stats.py (offline, once)
pandas>=2.0.0
pyarrow>=14.0.0

# --- Movie preview export (Workbench frames → MP4 + audio) ---
imageio>=2.34.0
imageio-ffmpeg>=0.5.1

# Optional legacy:
# kokoro-onnx>=0.4.0
